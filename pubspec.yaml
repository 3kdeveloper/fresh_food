name: fresh_food
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  get:
  provider:
  auto_size_text:
  introduction_screen:
  google_nav_bar:
  hive:
  hive_flutter:


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
  flutter_native_splash:
  build_runner:
  hive_generator:

flutter_native_splash:

  # For light theme
  color: "#ffffff"
  image: assets/logo.png

  # For dark theme
  color_dark: "#38372e"
  image_dart: assets/logo.png

  fullscreen: true
  android12: true


flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/icons/
    - assets/images/
